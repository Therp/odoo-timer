/**
 * templates.js — Desktop edition
 *
 * When the OWL XML templates are compiled (via scripts/compile-templates.sh)
 * this file is replaced with the compiled output and exports the template
 * registry.  Until then this stub ensures the module resolves cleanly so the
 * component files fall back to their built-in createXxxTemplate() functions.
 */
globalThis.__THERP_TIMER_TEMPLATES__ = globalThis.__THERP_TIMER_TEMPLATES__ || {};
